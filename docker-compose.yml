version: "3.9"

services:
  web:
    build: .
    image: arithmeticapi
    ports:
      - "5000:5000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:5000/3wina || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
